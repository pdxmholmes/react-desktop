export = ReactDesktopStylesWindows;
export as namespace ReactDesktopStylesWindows;

declare namespace ReactDesktopStylesWindows {
  type BackgroundProps = {
    background?: string | boolean;
  }

  type ColorProps = {
    color?: string | boolean;
  }

  type ThemeProps = {
    theme?: string;
  }
}
